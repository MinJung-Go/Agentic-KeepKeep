# UI 专项实施清单

状态：32屏设计已确认，SwiftUI开发完成，最终代码CI构建通过、271个测试通过；待真机视觉验收。分支 `feat/ui-refinement`。

主稿：[Figma全量32屏](https://www.figma.com/design/1KqpdNeLEQIdvd0tQn8wWR?node-id=33-2) · [本地预览](design.html) · [需求](requirements.md)。

## 设计与范围

- [x] 原稿16类界面全部覆盖，每类深浅两套；原17屏与首批6屏保留对照。
- [x] 本地独立HTML：资源内嵌、无脚本仍有内容，32屏无横向溢出，新增页面逐行视觉核对。
- [x] Figma可编辑主稿：页面36:2、画板33:2；976个文字节点。
- [x] 用户确认设计并授权进入App开发。
- [ ] Figma折叠说明残留文字：最后修正受Starter额度限制，本地正常；不阻塞已确认设计开发。

## 已实现

- [x] 共享主题：橙底深字、矩形圆角按钮、中性次级操作、禁用态、减少动态效果适配。
- [x] 用户聊天气泡采用中性色；发送按钮44pt；思考默认折叠且可以展开。
- [x] 首页课程优先；健康摘要可展开；概览改为紧凑文本；大字体健康指标单列。
- [x] 首页Logo、实底输入栏与图文发送保留，课程/健康无数据入口保留。
- [x] 最新记录确认固定在底部；旧待保存卡仍可保存；保存后保留只读草稿和回执。
- [x] 最新计划确认固定在底部，内联重复按钮隐藏；计划动作与组次按行对齐。
- [x] 确认回执、拒绝、失败提示和上下文整理状态沿用已有数据流；停止保留唯一入口。
- [x] 课程头部使用标题、周期、进度条；保留真实日期、训练日编辑和执行入口。
- [x] 记录列表增加搜索及无结果状态，共享记录行支持两行标题和摘要。
- [x] 力量趋势按动作查看，展示最新值、曲线与数据点；并列动作稳定排序。
- [x] 保留体重/训练容量/睡眠趋势、无数据提示和饮食目标算法；饮食辅助文字提升对比度。
- [x] 报告标题统一系统字体，保留可展开数据依据。
- [x] 引导与设置使用品牌资产，引导API Key输入遮挡。
- [x] 小组件保留小/中尺寸，增加锁屏矩形信息。
- [x] 训练执行与详情中的橙底标签统一为深字。

## 原生交互适配

- [x] 训练详情、训练日编辑继续使用真实数据绑定的原生表单及已有即时保存行为，不加入无实际作用的保存按钮。
- [x] 设计稿演示值不进入App；健康授权、错误状态与只读记录遵循已有行为。
- [x] 使用系统语义字体，Figma替代字体不嵌入App。

## 验证

- [x] `git diff --check`；本地设计JS语法检查。
- [x] 首轮提交77feba6的macOS构建通过。
- [x] 最终代码 `8b599b664b740195f5e83b0165c7ba7a7a0a2e05` macOS构建通过、271个测试通过、0失败：[CI日志](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/34820913194)。后续提交仅更新文档。
- [ ] 模拟器/真机检查：深浅模式、键盘、安全区、最大辅助字号、长对话、无计划、未授权、附图确认及连续记录。

本机没有Xcode，模拟器工具返回 `xcrun ENOENT`。浏览器设计稿验证不替代SwiftUI运行时验收。

## 交付

- [x] 开发分支已推送；为验证构建触发macOS CI。
- [ ] PR / 合并（尚未执行）。
- [x] 真机包 v0.4.2-ui-preview.1：代码0573d75，构建与271个测试通过、IPA归档成功；包版本/arm64/161张离线GIF/小组件校验通过，见[下载与真机验证](device-validation.md)。
- [ ] Figma组件库及完整原型连线（独立后续工作）。

## 原生质感复查（用户追加标准）

详见 [复查记录](native-ui-review.md)。以下为新一轮待办，前述CI通过不代表这些调整已完成。

- [x] 教练页只保留确认主按钮；拒绝移入更多菜单，输入聚焦时隐藏确认区，键盘工具栏可结束输入。
- [x] 次级操作改为文字按钮，保留44pt点击高度；本地稿低频操作降级。
- [x] 报告标题与正文去卡片外框；完整报告入口改导航行。
- [ ] 完成真实SwiftUI运行时视觉及动效验收。
