# Milo 体验实施清单

第 14、15、16 轮已合并。本阶段仅维护本地设计稿，暂不需要同步 Figma。

## 推理与工具进度（已开发）

- [x] GLM medium 与显式关闭思考，兼容其他模型
- [x] 联网默认开启及已保存偏好兼容
- [x] 真实工具进度、失败/取消状态及耗时
- [x] 进度折叠回看、本地持久化、不进入模型上下文
- [x] 编码参数、默认值、工具事件回归测试
- [ ] Xcode 构建与完整单测
- [ ] 真机：推理等待时间、联网开关、进度展示和旧记录兼容

- [x] 上下文默认 128K，实际输入预算随窗口变化，保留手动设置
- [x] 本地 115 个 Swift 文件语法检查、差异检查和旧素材检查通过

新增 8 项回归测试，另更新联网默认值与窗口设置测试；Xcode 运行结果待填入。未发起真实模型或联网搜索请求，尚未验证实际延迟和真机视觉。

功能代码已随 `5de87cd` 提交并推送到 `feat/glm-speed-tool-activity`；此处的 Xcode 与真机待验证状态保持不变。

## 角色与对话设计（设计阶段）

- [x] 内置 image_gen 生成 Milo，用户认可素材
- [x] 透明原图保存在 assets/milo.png（1254 × 1254 RGBA）
- [x] 撤回提前进行的聊天页 Swift 修改；资源移出 App 资源目录
- [x] 高保真 HTML：初次见面、欢迎页、查询回复、键盘展开
- [x] 深浅主题、昵称实时预览、快捷问题填入、查询过程展开、示意键盘收起
- [x] 浏览器渲染检查浅色四屏；改进图标与回复页间距
- [ ] 用户确认设计
- [ ] 确认后实施 iOS、构建与真机验证

角色与 UI 部分仅为设计预览，随本次提交归档；未进行 iOS 开发、iOS 单测、推送或 IPA 构建。

### 首页记录栏设计调整

- [x] 原稿首页深浅色记录栏统一轻量胶囊与导航间距
- [x] 空输入隐藏发送；文字或照片触发送出状态；提交后恢复
- [x] 浏览器实际执行 5 项交互检查全部通过：空白、文字、文字发送复位、仅照片、照片发送复位
- [x] JavaScript 语法检查通过；浏览器截图检查深浅色
- [ ] 用户确认后再实施 iOS

## 搜索扩展与网页阅读（已开发）

- [x] 搜索默认10条，count支持1–50，参数越界钳制
- [x] 解码按请求条数保留结果，搜索工具转交上限64KiB，响应上限2MiB
- [x] 来源去重、HTTPS校验、每条摘要280字节与总上下文预算保留
- [x] 接入智谱 /reader，使用纯文本、不保留图片
- [x] 仅从本轮搜索结果通过 source_index 选取来源；地址额外校验
- [x] 网页正文每页最多16,000字节、响应最多2MiB；每轮最多2次阅读
- [x] 支持本地查询→搜索→阅读→回答；保留次数、失败与取消处理
- [x] 展示真实“阅读公开网页”进度；失败和空正文不标为成功
- [x] 条数和来源编号严格解码，拒绝超大/小数参数
- [x] 新增8项单测（搜索1项、阅读7项）；更新原搜索请求与结果测试
- [x] 本地可移植 SwiftPM 套件实际运行41项，0失败
- [x] 117个Swift文件语法检查及git diff --check通过
- [ ] Xcode完整构建与单测
- [ ] 真实GLM账号联调、iPhone进度与正文引用验证

### 验证边界

测试在Linux临时SwiftPM harness运行，复制本轮工具和网络源码，增加FoundationNetworking与URLSession.AsyncBytes测试适配；使用URLProtocol固定响应，无真实付费API调用。不等同于iOS SDK编译、原生流式网络或真机验证。搜索扩展与网页阅读代码随本次本地提交归档，未推送、未运行对应 CI、未打包 IPA。此前推理与工具进度提交仍为 `5de87cd`。

官网已核对网页阅读ReaderRequest与ReaderResponse.reader_result.content字段；未假定支持登录网页、验证码绕过或整站递归抓取。

## 文档整理

- [x] 合并原第 14、15、16 轮需求、checklist、HTML、预览图与 Milo 原图至第 14 轮
- [x] 更新文档入口，移除独立第 15、16 轮目录
- [x] Figma 同步移出本阶段范围
