# 第 30 轮 · Checklist

## 当前追加：GLM Realtime 语音／视频通话

- [x] 更新需求：默认语音、可选摄像头、原语音入口保留。
- [x] 制作实时通话稿，覆盖入口、首次说明、仅语音、视频、静音、字幕、打断、镜头切换、权限拒绝、断线与课程建议。
- [x] Chromium 验证默认不开摄像头、开启确认／取消、镜头切换、静音与画面独立、打断保持静音、断线后仅恢复语音、字幕、课程提示、旧入口、拒绝权限、挂断及 390px 窄屏；无脚本错误。
- [x] 生成 design.realtime.voice.png、design.realtime.video.png、design.realtime.dark.png，并检查语音／视频截图。
- [x] 用户确认实时通话 UI 并授权 App 与 Service 开发。
- [x] 核对官方输入 WAV／输出 PCM 和视频模式协议；采用服务端代理，无需下发临时供应商凭证。
- [ ] 确认实际模型账号的 Realtime 权限、额度及费用，并完成真实上游联调。
- [x] Service：固定上游 WebSocket、账号鉴权、会话撤销、限额与消息校验。
- [x] App：双向流式音频、字幕、自动／手动打断与重连。
- [x] 摄像头：按需授权、前后镜头、JPEG 帧发送、关闭后恢复文字上下文。
- [x] 聊天接入：保留旧入口、保存通话文字、课程建议转入原确认流程。
- [x] 单测：未授权／撤销／限流／无密钥泄漏／协议边界和生命周期。
- [x] 同步 Service 部署说明与配置；本轮已获用户部署与打包授权。
- [x] iOS 构建与单元测试：448 项通过、0 失败。
- [ ] 真机验证自然插话、视频、音频路由和生命周期。

输入框转写提案已归档为 [design.input.html](design.input.html)，暂缓；不再执行关闭原连续语音的清单。

## 历史：0.5.6 连续语音实现与验证

- [x] 从 main 创建 `feat/milo-voice-chat`。
- [x] 检查现有聊天、语音识别及权限文案。
- [x] 建立需求基线，标记尚未确认的交互范围。
- [x] 用户确认连续语音方向：自动收音、语音回复；具体打断与权限等细节列入设计提案。
- [x] 完成[原语音交互稿](design.continuous.html)，覆盖收音、思考、播放、静音、打断、权限拒绝、断网、系统中断及课程确认；含浅深色与窄屏适配。
- [x] 参考豆包公开介绍与界面，调整为通话布局、字幕开关和圆形控制；演示按钮移出手机。
- [x] 用户确认更新后的具体设计稿及点击打断范围。
- [x] 按确认范围实现语音会话状态与权限／生命周期处理。
- [x] 接入聊天页面，保留草稿及现有工具确认行为。
- [x] 更新数据使用和系统权限说明。
- [x] 新增并通过 14 项语音状态／朗读文本测试（Linux、音频 API 替身）。
- [x] iOS 构建与完整回归测试：444 项通过、0 失败（含 14 项语音测试）。
- [ ] 真机验证权限、识别、音频路由与中断恢复。

## 验证记录

- 设计阶段：Chromium 验证字幕、异常说明、静音、打断、正常对话循环、首次说明、结束返回和 390px 窄屏；浅深色截图见 design.light.png / design.dark.png。
- 实现阶段：Swift 6.0.3（Swift 5 模式）运行 `VoiceConversationTests`，14 项通过、0 失败。使用实际协调器、文本处理及测试文件；识别／朗读注入测试替身，Linux 的 AVFoundation／Combine 桩仅用于运行状态测试，不代表 Apple SDK 验证。
- 已执行全部修改 Swift 文件的语法解析、Info.plist 格式检查和 diff 空白检查。
- CI 提交号：`d0b2de90e5a7608452706357a71bfeef3c7df0a6`；[iOS CI](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35857434790) 已通过构建和 444 项单测。SwiftUI 真机展示及真实录音／播放仍待装机验证。
- IPA：0.5.6（27）归档完成，[下载产物](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35857434790/artifacts/10750037343)。已下载校验包完整性、版本号与语音权限声明；大小 8,779,366 字节，SHA-256 `cf782b490f3a0c21ac1a12e2656b1668b71f70ea30fc51d818fc5fa354689fb9`。需要 AltStore／Sideloadly 重签名安装。
- 真机重点：首次权限与拒绝后重试、连续中文收音、系统音色、扬声器／蓝牙切换、来电／后台暂停、两处入口退出及课程确认后继续。

## 多入口追加

- [x] 核对首页 Milo 与课程页 AI 教练共用 `CoachChatView`，记录框使用独立 `QuickLogSheet`。
- [x] 用户确认另一入口为课程页 AI 教练，与首页 Milo 共用本轮语音设计。
- [ ] 验证首页与课程页均可进入连续语音，挂断回到当前聊天，关闭聊天回到原页面。
- [ ] 验证两入口的字幕、静音、打断、权限与资源释放，以及课程草稿手动确认行为。

## GLM Realtime 实现验证（本轮）

- App：共用聊天页提供实时／原语音两入口；实时摄像头默认关闭，支持字幕、静音、打断、摄像头授权／切换／关闭、课程建议转回文字聊天。
- Service：在现有 `feat/admin-console` 分支接入（保留已部署管理台和加密 Key 功能），固定上游 WebSocket 转发，无客户端供应商凭据。需部署时显式配置 `REALTIME_ENABLED=true`。
- 后端：30 项测试通过、0 失败（含 10 项实时连接测试）；未连接真实 GLM，不代表模型账号权限和额度已验证。
- Swift：4 项 RealtimeWire 协议测试通过；新增与修改文件通过 Swift 语法解析。后续 Apple SDK 编译与 448 项 iOS 单测均已通过。
- App 提交 `28afffd` 已推送；0.5.7（28）[CI 与 IPA 构建](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35954333740) 编译与 448 项单测通过，IPA 归档完成。上一版 444 项 iOS 测试只对应 0.5.6。
- Service 提交 `2d48fa1` 已推送并部署；30 项回归测试通过。生产容器 healthy，公网健康检查与 WebSocket 未授权拒绝验证通过；真实 GLM 握手与会话配置成功，临时账号通过鉴权代理收到语音与视频模式 ready；测试数据已清理，尚未验证真实音视频。
- 待验证：实时调用权限和费用、真机 AEC／自然插话／摄像头／蓝牙／网络中断，以及视频切回语音的上下文恢复。

## 0.5.7 部署与 IPA（2026-09-24）

- [x] 用户授权提交、推送、云端部署与 IPA 打包。
- [x] App `28afffda2a48c388dbff5df5c50954e4497c251c`：CI 全部成功，448 项单测通过、0 失败。
- [x] [0.5.7（28）IPA](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35954333740/artifacts/10790217486) 已下载校验：ZIP 完整、版本正确、服务地址仍为公网 IP、麦克风和摄像头权限声明存在。需侧载重签名。
- 大小：8839383 字节；SHA-256：`fa1231ffe0d46a0321f2cda2571ce4d4dc9ff073cfb24d5caec2714b76a205ca`。
- [x] Service `2d48fa1` 镜像已部署，文档提交 `197d90a` 已推送；未触发 Service Actions。部署及真实代理验证见独立服务清单。
- [ ] 真机验证音频路由、自然插话、摄像头与后台恢复；本轮未声称完成这些验证。

## 语音输入与实时故障纠正

- [x] 记录用户纠正：普通入口只转写，实时通话自然打断。
- [x] 输入框转写、停止／取消与草稿保留；移除聊天页旧连续语音接入。
- [x] 移除手动打断控件；保留 server_vad 语音起始事件停止播放逻辑。真机回声与插话效果待验证。
- [x] 真实音频复现 GLM 推理 422：工具参数缺少 description。修复后直连与鉴权代理正常返回音频；Service ef489db 已部署。
- [x] 区分每分钟／每日限流、旧会话占用、服务关闭等错误；不能断言此前每次重连失败都由限流导致。
- [x] 单测与语法检查；Apple SDK 构建成功，454 项单测通过。真机验证仍待完成。

- 0.5.8（29）App `fd074ed` 已推送，[CI / IPA](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35962255804) 全部成功。Linux 10 项转写与协议测试通过，Swift 语法解析通过；Service 33 项单测通过。

## 0.5.8 交付验证

- [x] CI `fd074ede263260016c5d01b91dd05e94d5f2f17b` 构建、454 项测试及 IPA 归档成功，0 失败。
- [x] [下载 0.5.8（29）IPA](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35962255804/artifacts/10793073052)：已下载并验证 ZIP 完整性、版本号、IP 服务地址、麦克风／摄像头／语音识别权限。
- IPA 大小 8818677 字节，SHA-256 `8f25fcf0ddab267f00709a8830b0b29364ac498c9b3eac119140978bb4a8244d`。需 AltStore／Sideloadly 重签名。
- [x] Service `ef489db` 已构建部署；真实合成语音经鉴权代理两轮识别，返回 483674 字节音频、0 错误。文档提交 `4c93821` 已推送。
- [ ] iPhone 实际转写、扬声器／蓝牙回声抑制及自然插话；合成音频未观察到明确 cancelled 状态，不能代替真实播放中的插话验证。

## 真机说话无反应诊断

- [x] 当前生产密钥经代理两轮合成语音成功；公网地址加聊天历史复测成功。仅证明服务链路可用。
- [x] 检查 App：连接成功即显示收音状态、转换失败静默返回，缺少采集与发送有效性的诊断。尚未确认是真机根因。
- [x] 增加可关闭、无内容的服务端计数诊断；38 项服务端测试通过。生产临时启用后合成测试 133 帧／2 次语音起始／510980 字节回复正常，等待真机证据。
- [ ] 获取真机复现的音频帧／音量／VAD／回复证据后定位根因。
- [ ] 修复已证实问题并完成相应测试；真机验收单独记录。

- 用户反馈 App 无法连接后，已撤回生产临时诊断挂载，恢复 ef489db；公网内外 health 200，继续核查实时接口与连接限额。

## 采集链路修复

- [x] 只读流量与用户反馈对应：上行停止增长、下行仍有回复；采集／转换具体根因需新版本验证。
- [x] 明确麦克风 → 静音 mixer → 主 mixer 连接；监听分支音量为 0，保留 VoiceProcessing。实际收音与回声待真机验证。
- [x] 转换器按实际 buffer 格式初始化，输出 PCM16／16k／单声道；转换失败上报，不再静默忽略。
- [x] 增加无采集 5 秒／发送停滞 8 秒检测，排除正常安静、静音和切换；5 项状态测试通过。
- [x] 音频转换与检测状态测试、Apple SDK 编译：原生 Apple 音频检查 10 项通过，完整 iOS 测试 464 项通过、0 失败。
- [x] 正式 Git 提交、推送与 0.5.9（30）IPA 构建完成；真机验收单独记录，不宣称已修复真机。

- 首次 CI `45853dc`：Apple 编译通过，464 项测试中两项新增音频测试产生 4 个断言失败。实际重采样有有效输出；失败来自首批采样数断言未考虑转换器内部缓存（48k 首块 1365、44.1k 首块 1486 样本）。修正为连续 10 秒的样本总量／信号验证，并允许最多一个输入周期的缓存，同时限制不得重复输出。没有放宽音频是否有效的验证。新增先行的原生 Apple 音频测试，完整 iOS 测试继续保留。

## 0.5.9 交付验证

- App 采集修复 `45853dc`、连续音频验证与先行检查 `9ab4c7d487901121f36ce4e3a354ac91639fb278` 已推送至 `feat/milo-voice-chat`。
- [CI](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35973455971) 全部成功：10 项 macOS 原生音频检查、464 项 iOS 单测通过，0 失败；IPA 归档完成。
- [0.5.9（30）IPA](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35973455971/artifacts/10798006793) 已下载校验：ZIP 完整；App／Widget 版本正确；服务地址为 `http://47.100.234.212:8080`；麦克风、相机、语音识别权限声明存在。需侧载重签名。
- IPA 大小：8823280 字节；SHA-256：`e932e7c91363297b18c852effbf515ca446b15fb2035209f4185f31beb8ce610`。
- 线上 Service 维持 `ef489db`，临时诊断挂载已撤回，只读网络统计已停止。Service 本地诊断草稿未部署、未提交；不声称两个仓库均已推送。
- [ ] 真机确认默认仅语音状态下连续收音、正确识别、回复与自然插话；若仍无采集或上传，记录新版具体错误提示。当前只能证明代码与合成转换验证通过，不能以此代替设备验收。

## 0.5.9 真机未通过：内置麦克风

- 用户确认没有耳机，提示“没有收到麦克风音频”；0.5.9 未解决该设备采集问题。此前合成转换、CI、IPA 成功不能视作真机通过。
- [x] 区分原始 tap、非空采样、转换输出和引擎停止，诊断只保存时间戳，不记录语音或凭证。
- [x] 对仍在活动通话中但已停止的音频引擎做一次恢复，保留回声处理与通话身份。
- [x] 本地阶段诊断／有限恢复策略 7 项测试与既有健康检测 5 项测试通过，0 失败。
- [ ] Apple SDK 编译、完整 iOS 测试与 0.5.10（31）诊断 IPA。
- [ ] 真机对照输入框语音转文字，并验证连续收音／回复／自然插话。
- 参考 Apple 官方 [Using voice processing](https://developer.apple.com/documentation/avfaudio/using-voice-processing) 示例：监听引擎配置变化并检查运行状态。尚无设备证据证明本次就是引擎停止。

## 约两分钟断开修复

- 用户确认收音已可用，新增反馈为约两分钟后断开。
- [x] 核对生产独立实时上限 600 秒、普通模型超时 120 秒；当前生产镜像 ef489db，健康。
- [x] 发现前台通话未禁用自动锁屏，进入后台会暂停通话；不能在未获取具体错误前声称此为唯一原因。
- [x] 生产鉴权代理每 12 秒合成发言，连续 160 秒：13 次真实转写，120 秒后仍有 3 次转写／543840 字节回复，0 错误。
- [x] 前台连接和通话持有屏幕唤醒，断开／暂停／失败恢复原值；新增 4 项测试通过，连同采集状态共 16 项 Linux 测试通过。
- [x] App 提交推送，Apple CI 和新 IPA 完成。
- [ ] 真机两分钟以上连续通话、较长停顿恢复与前台防自动锁屏验收。

- 首次长测：两次发言后持续上传静音，在 68 秒收到 relay.error，尚未取得具体终止类别；不能认定服务无长通话问题。正在以每 25 秒发言复测。

- 进一步对照：上游主动发送 call.hangup，end_reason_code=silence_timeout。正确初始化顺序为 session.created → session.update → session.updated；保留课程工具的直连合成长测 160 秒成功，含两分钟后的真实转写与对应回复。尚待生产代理和真机长通话验收。
- Service 修复提交 f326278，本地完整 41 项测试通过；正式镜像由同提交归档构建，尚待推送确认后切换生产。App 修复提交 97553e7，[CI / IPA](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/36008397104) 正在运行。

- Service f326278 已推送并正式部署（镜像 moveliq-service:f326278），公网健康正常，无源码挂载；41 项服务端测试通过。生产长测 160357 ms，15 次语音起始、14 次停止、13 次转写、14 次回复、2300172 字节音频，0 错误。保留课程工具和管理员 600 秒时长限制，未变更密钥。
- App 97553e7：21 项 Apple 原生音频检查、475 项 iOS 单测通过，0 失败；0.5.11（32）IPA 归档中。

## 0.5.11（32）交付

- App 源码 97553e78a1f5bc83c3f19b9763ec9a352fb6489d，CI 36008397104 全部成功：475 项 iOS 测试、21 项 Apple 原生音频检查通过，0 失败。
- [下载 IPA](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/36008397104/artifacts/10812578328)：已下载并验证 ZIP 完整、App／Widget 版本 0.5.11（32）、IP 服务地址和麦克风／相机／语音识别权限；需侧载重签名。
- IPA 大小 8823709 字节；SHA-256 a01273c10c87adf0137eaafa42ea3300f164d140a79ff118aa6bc60ad907abb2。
- 每 25 秒发言复测仍发现上游静默挂断，补充仅对 silence_timeout 的有限恢复；Service a0d0672 已推送并由同提交构建正式部署。48 项服务端测试通过，不运行 Service Actions；带停顿的 160 秒生产验收通过。

- 最终生产 a0d0672，--long --pauses 实测 160395 ms：2 次静默恢复，App WebSocket 未断；6 次真实转写／6 次语音停止／12 次回复／2132546 字节音频，0 错误；120 秒后仍有 1 次转写、696570 字节音频。临时账号和限额记录已清理。
- 公网健康正常；不修改密钥、管理员 600 秒总时长或数据库结构。IPA 仍为上述已验证的 0.5.11（32），补充恢复逻辑仅在 Service，不需要重建 App。真机前台自动锁屏与较长停顿体验待用户装机确认。

## 0.5.12 修复：录音中发送与通话长回复（2026-09-25）

- [x] 记录用户现象：聊天页必须先停识别才能发送；通话中长语音回复会直接中断，提示为「实时连接已中断，请检查网络后重试。」。
- [x] 定位问题一：`canSend` 排除了 `dictation.active`，首页「记一笔」没有这个限制。
- [x] 聊天页录音中可直接发送：`VoiceInput.finishForSend()` 取走已识别文字并结束这次收音；`startSend()` 不再要求先手动停；不自动发送、不朗读、取消行为不变。
- [x] 新增 2 项 `VoiceInputTests`：录音中取文发送、停止后旧回调不写入。
- [x] 定位问题二：服务端断连均带具体文案，用户看到兜底文案说明传输层被拆；App 内三条与长回复相关的中断路径已核出（下行积压抛错、上行队列满挂断、字幕逐块重排）。
- [x] 下行播放：新增 `RealtimePlaybackQueue`（1 秒切块、20 秒积压上限、超限丢新块），`RealtimeAudio.play` 不再因积压抛错；丢弃一次时提示「播放跟不上，已跳过一段语音。」。
- [x] 上行队列：满时先丢最早的麦克风音频帧，控制消息排不出去才提示「网络过慢」。
- [x] 通话字幕合并到 60ms 窗口刷新，结束/打断时补刷最后一段。
- [x] 断连提示补传输码：`RealtimeWire.transportNote` 输出 `（对端关闭 N）`／`（连接错误 N）`，接收与发送两条失败路径都带上。
- [x] 新增 6 项 `RealtimePlaybackQueueTests`、1 项 `RealtimeWireTests`；播放积压测试纳入 macOS 音频检查 job。
- [x] 设计稿与需求同步：design.input.html 改为录音中可直接发送，requirements.md 增补本轮纠正。
- [x] Apple CI 构建与完整单测通过：提交 `65a7260`，[CI 36037223725](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/36037223725) 全部成功。macOS 音频检查 28 项（含新增 7 项播放积压测试）、iOS 单测 485 项，0 失败。上一版为 21 项音频检查／475 项 iOS 单测。
- [x] [0.5.12（33）IPA](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/36037223725/artifacts) 已下载校验：`verify_ipa.py` 全部断言通过；App 与 Widget 版本 0.5.12（33）；服务地址 `http://47.100.234.212:8080`；麦克风、相机、语音识别权限声明存在。需侧载重签名。
- IPA 大小 8828600 字节；SHA-256 `6b9aebc9520148ff08a498fc9af581b493c2c2a895eb172819bed64124a70b0f`。
- [ ] 真机验证：录音中发送、长回复通话是否仍会断、断连时出现的传输码（`（对端关闭 N）`／`（连接错误 N）`）。
- 未在本次改动：通话看门狗仍以主线程时间戳判断采集／上传是否停滞，主线程被长时间占住时理论上仍可能误判；本次未见该路径的提示，未改。
