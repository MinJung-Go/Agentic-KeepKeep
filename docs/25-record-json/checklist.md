# Checklist

- [x] 记录现象、范围与验收标准，无新 UI。
- [x] 添加失败用例，复现错误包装、字段诊断丢失与本地格式恢复缺口。
- [x] 修复按顶层形态解码与安全诊断。
- [x] 本地完整围栏兼容和一次格式重试，保留图片与原文。
- [x] 测试重试上限、取消、内存、空结果与云端行为。
- [x] 执行 portable 回归并记录结果。
- [ ] Apple CI 构建、iPhone 真实图片识别与确认保存回归。

## 验证证据
- 基线源码：`de827bc`。本轮改动尚未提交／推送，也未打新 IPA。
- 修复前：64 项 portable XCTest，新增用例产生 5 个失败断言／异常（4 个测试用例失败）。合成响应 `{"records":"wrong"}` 复现两层 `invalidJSON` 和字段路径丢失；这不是用户原始模型响应。
- 修复后：Swift 6.0.3 Linux portable XCTest **69 项通过、0 失败**，其中 10 项记录恢复测试，覆盖纯文字、多轮历史、图片附件保留、格式重试上限、空数组、内存、取消与网络错误。
- 云端原有 thinking 配置与不重试行为保持；Milo 聊天路径未改。
- 新增本地 JSON 围栏规范化用例；截断、多对象、围栏不完整、思考标签仍拒绝。JSON 模式消费规范化内容；普通聊天不变。
- portable 脚本使用真实 ParserAgent、DTO、提示词、解码器和既有 MockLLMClient；ChatRole 从源文件原样提取以避开 Linux 不支持的 SwiftData 文件，不伪造模型输出能力。
- `git diff --check`、LocalLLMClient Swift 语法解析通过。当前未执行 Apple SDK 类型检查、完整 iOS CI 或实际 MLX 权重推理。
- 真机需复测：纯文字“深蹲100kg 5×5”、饮食文字、多轮“再加一组”、单张食物照片；确认草稿及最终保存正确。不能据模拟响应测试宣称模型记录成功率或真机问题已解决。
