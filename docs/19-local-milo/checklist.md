# 第 19 轮 · 离线 Milo 验证清单

对应 [需求文档](requirements.md)。当前分支 `feat/local-milo-validation`，2026-09-18 建立。

## 本轮开发记录 · 2026-09-18

用户已确认 v3 六场景与异常交互并要求开始开发；无需再次确认。以下勾选表示对应实现或本地验证完成，不代表 iOS CI／真机通过。

- [x] Qwen3.5-2B Q4_K_M + F16 视觉模型清单、固定 revision／SHA-256，实际合计 1,949,063,104 字节；权重在临时目录验证，不进仓库／IPA。
- [x] llama.cpp／libmtmd 固定 commit，C++ 桥接、本地 LLM 适配、8K 预算、非思考模板、XML 工具解析、单图预处理。
- [x] 下载、文件校验、暂停／恢复、蜂窝确认、删除、模型管理与首页入口接入。
- [x] 内置可见 WKWebView、公开主题搜索、受限来源阅读、用户确认提取、取消／超时；不自动回退付费 API。
- [x] 图文记录提案接入；失败文字与图片保存为待归类笔记，重试图片进入记录确认页，导出包含附件。
- [x] App 后台／内存压力卸载，离线模式不自动调用云端视频补全。
- [x] 生成 24 秒设计短片、GIF 预览与封面，放在 README 最开头，明确非真机录屏。
- [x] macOS 编译／项目全量测试：最终 CI 379 项通过，IPA 归档成功。
- [ ] iPhone 性能、下载中断、WKWebView 和图文实机验收：待设备信息和真机。
- [x] 130×3 首轮真实模型生成、格式解析与原始结果归档；JSON 语法、个人事实和日期均发现失败。
- [ ] 完整工具链与人工任务质量评测；当前未达到产品化门槛。

详细证据、用例与失败输出见 [验证记录](validation/README.md)。

## 0 · 需求与基线

- [x] 先建立需求文档，明确验证范围、模型候选、拟定验收标准和不做事项。
- [x] 再拆解 checklist，区分设计、开发、自动验证和真机验证。
- [ ] 收集目标 iPhone 型号、iOS 版本、安装签名方式与可用空间，确定内存预算（R5）。
- [x] 核验 Qwen 语言量化及匹配视觉组件的全部下载文件，锁定 revision／SHA-256／实际大小／许可与 NOTICE（R1）。
- [x] 锁定 llama.cpp／libmtmd commit，核验 iOS 最低系统版本与 Metal 编译链接条件（R1）；真机部署性能另验。
- [x] 固定模板、关闭思考方式、8K 总预算、输出上限、采样参数与测量口径（R1）。

## 1 · 验证准备（不依赖 UI 设计）

- [x] 创建固定合成数据与隔离测试库，准备 30 条解析、20 条查询、20 条课程表、20 条对话、10 条异常用例及预期结果（R4）。
- [x] 定义结构化结果、关键字段整条正确率、任务通过率、人工评分标准与失败样例记录格式（R4）。
- [x] 准备结果表：模型／运行时版本、设备、上下文、首字与首段正文延迟、吞吐、内存、thermalState（R5）。

## 2 · UI 设计（必须先于对应界面开发）

- [x] 本地 `design.html`：首次下载提示、稍后再说、蜂窝选择、空间不足（R2）。
- [x] 下载／校验／恢复／失败重试／加载状态、模型删除与重新下载设计（R2）。
- [x] 旧 v2：本地不可用、图片不支持、主动切换云端提示（已由 v3 图文设计替代）；保持 Milo 与现有浅深色风格（R2、R3）。
- [x] 用户确认具体设计稿，记录范围与日期；确认前不接入这些界面。

## 3 · 运行时与工具适配

- [x] iOS 编译链接 llama.cpp／Metal（R1、R3）。
- [ ] 真机完成最小加载与生成（R1、R3）。
- [x] 实现本地 LLM 适配：后台加载、流式输出、取消、错误处理与单任务串行（R3）。
- [ ] 核验非思考模式确实不产生隐藏思考；长请求执行预算与工具结果裁剪（R1、R3）。
- [x] 接入 Milo 身份、时间／时区、只读健康摘要及按需记录查询（R3）。
- [x] 适配工具模板与解析、白名单及参数校验、轮数上限；维持写入确认与幂等（R3）。
- [x] 隔离本地和云端模式：不得自动外发；图片及联网工具不冒充离线能力（R3）。
- [x] 生命周期处理：后台切换、内存压力、卸载、取消后重试（R3、R5）。

## 4 · 下载与设置（UI 确认后实施）

- [x] 下载清单与可信来源，存储空间及蜂窝选择（R2）。
- [x] 下载取消／恢复／重试，服务端文件变化、Range 不支持的处理（R2）。
- [x] 临时文件校验、原子切换、排除备份与删除模型；保留用户数据（R2）。
- [x] 接入已确认的首次提示与模型管理状态，不阻塞原有手动记录（R2）。

## 5 · 自动验证与 CI

- [ ] 下载状态机与损坏文件测试：不足空间、中断、恢复异常、校验失败不进入可用态（R2）。
- [ ] 工具解析／非法参数／未知工具／取消／重复写入／超预算回归测试（R3）。
- [x] 固定 100 条文本和 30 条图文各跑 3 次首轮真实生成（390 次）；JSON 修复追加 180 次；全部原始输出归档。
- [ ] 完整 App 工具链和人工任务评分（R4）：首轮质量未过门槛，尚未达到端到端验收。
- [x] 运行项目相关测试与 iOS CI：`06135e6`，379 项、0 失败；最终运行链接见下。
- [x] 已打验证 IPA 0.4.11（17），核对原 Bundle ID、内嵌引擎和许可证，包内无模型权重。

## 6 · 真机验收与决策

- [ ] 飞行模式聊天、解析与本地查询；写入需确认且不损失输入（R3）。
- [ ] 冷加载 5 次、热短问答 30 次及 2K／8K 工具输入的延迟与内存测量（R5）。
- [ ] 100 次请求稳定性、连续使用 15 分钟、取消／锁屏／后台／中断恢复（R5）。
- [ ] 检查下载、设置和错误状态的浅深色真机体验（R2）。
- [x] 整理首轮任务质量、失败案例、硬件及照片／联网证据缺口（R4、R6）；完整端到端与真机仍未验收。
- [ ] 正式锁定前复核最新轻量 Qwen 图文发布；当前候选 Qwen3.5-2B，不采用 Spark。
- [x] 建议暂不以当前配置替代云端主模型，仅交付受限验证 IPA；是否迁移 MLX／调整模型，由后续验证和用户决定。

## 验证记录

已接入本轮 App UI、下载和运行时；Linux 真实模型 smoke 与 12 项纯 Swift XCTest 已执行。iOS CI 已通过，完整端到端评测及真机未验收，已有验证 IPA；默认 ModelScope 与 JSON 约束的 0.4.11（17）构建通过。性能与正确率目标尚未验收。

### 下载 UI 设计 · 2026-09-18

- [三场景交互稿](design.html) · [浅色预览](preview-light.png) · [深色预览](preview-dark.png)。
- v2 改为首页半屏提示、首页紧凑状态、设置管理三个场景。下载／暂停／校验／完成／异常复用同一状态区域，不再逐步切换整页。顶部选择器展示各状态。
- Chromium 检查通过：图片加载、浅深色切换、蜂窝确认、暂停继续、异常状态切换、删除确认及 390px 页面／内容宽度。已视觉检查浅色整体排版。
- 全部按钮仅模拟状态，不下载、不读取空间、不调用模型；数值明确标为示意。
- v3 已获用户确认，本轮开始实施。

### UI 精简 v2 · 2026-09-18

- [x] 根据用户反馈重做：首次使用半屏提示，Milo 只用于邀请和既有首页身份；过程不重复大头像。
- [x] 精简文案为“没网，也能和 Milo 聊聊”“下载并开启”“暂不开启”；保留体积与离线能力边界，移除 API Key、系统调度、重复阶段说明。
- [x] 暂停移至进度旁；取消放入下载选项；校验与就绪复用首页状态，首次加载可由状态选择器体验。
- [x] 更新浅深截图；Chromium 检查蜂窝取消／同意、暂停继续、七种后续状态、删除与 390px 排版通过，视觉核对浅色三场景。
- [x] v2 已由用户确认的 v3 替代。

## 7 · Qwen 图文与内置浏览器追加 · 2026-09-18

- [x] 更新需求：取消 Spark，采用 Qwen 图文路线，内置 WKWebView 搜索／阅读；无付费搜索 API 依赖。
- [x] 明确 Qwen3.5-2B 为待验证端侧候选，与最新 Qwen3.8 大型号区分；删除旧下载体积承诺。
- [x] 锁定语言与视觉文件清单、配套 revision、许可、哈希和实际总大小；全部通过再标记图文就绪（R1、R2）。
- [x] 增加图片方向修正、缩放、像素／视觉 token 预算、单图＋文字输入和失败保留；实现离线图文推理（R1、R3）。
- [x] 补充 UI：照片提案与失败、图文下载说明、内置浏览器、验证码手动操作、来源和心流；更新浅深截图（R2、R7）。
- [x] 用户确认 v3 六场景与异常交互后再开发对应 UI。
- [ ] 选定搜索站点并验证网络可达性、使用规则与提取方式，记录失败与替代路径（R7）。
- [x] 实现受限 search/read 工具、固定 DOM 提取、真实来源引用、结果和工具轮数预算（R7）。
- [x] 实现可见 WKWebView、导航／重定向／子资源限制、数据隔离、取消与用户接管（R7）。
- [ ] 测试多文件缺失／错配、图片损坏／超限、视觉 token 预算、取消及图文峰值内存（R1、R2、R5）。
- [ ] 跑 30 条图文和至少 20 条浏览器用例；包含网页提示注入、私网 URL、敏感信息外发和引用一致性（R4、R7）。
- [ ] 真机验证飞行模式图文、联网搜索与阅读；文本和视觉性能分别报告（R5、R7）。

v3 已确认并实现，当前验证状态以本文顶部与 validation/README.md 为准。

### UI 补稿 v3 · 2026-09-18

- [x] 六场景：下载邀请、下载进度、模型管理、照片记录、搜索回答、内置浏览器。
- [x] 图文状态：待确认记录、照片模糊、视觉文件缺失、图片处理失败；原输入保留，可选择文字路径。
- [x] 网页状态：搜索结果、来源阅读、用户验证、断网、无结果、超时；可返回对话。
- [x] 完成后的心流位于正文之前，默认收起；来源可进入浏览器，读取后返回对话。
- [x] 更新 preview-light.png／preview-dark.png；Chromium 检查图片、浅深切换、下载交互、全部新增状态、来源阅读及 390px 无横向溢出，通过。人工查看新增三屏浅色排版，修正来源域名与标题挤在同一行的问题。
- [x] 用户确认 v3（2026-09-18）。HTML 仍是交互原型；App 实现单独验证，不代表 iOS／真机已通过。


### 追加实现与验证 · 2026-09-18

- [x] 用户授权提交、推送和 IPA 打包；全部使用 MinJung-Go 身份。
- [x] 完成 iOS 编译、框架链接与全量单测；首轮 `17d5962` 372 项通过，随后追加图片、存储预算、下载取消和浏览器测试。
- [x] 20 个网址策略向量、搜索结果裁剪和不可信标记通过；真实 WKWebView 网络交互仍待真机。
- [x] 实际检查 IPA：引擎确实内嵌、模型权重未打包、保留 Bundle ID 与许可证。
- [x] ModelScope 默认源：固定镜像 revision，两文件元数据大小／SHA-256 与已下载实测权重一致，Range 206 探测通过；续传文件按来源隔离。
- [x] JSON grammar 接入 jsonMode，修复真实模型输出缺括号的问题；事实质量独立评估。
- [x] 整理 [苹果端 MLX 候选](apple-runtime-options.md)，未擅自把已编译引擎替换为未验证的新路线。
- [x] 最终 0.4.11（17）：[CI 与 IPA](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35312358728)；源码 `06135e6cd42092fb03f6800a87010ae033bc52f7`，379 项测试、0 失败。IPA 5,182,144 字节，推理框架二进制 8,140,768 字节，最低 iOS 17。

未完成项中，真机性能／内存／发热、实际 WKWebView 联网、下载杀进程恢复需要设备；完整工具循环和人工质量验收尚未通过。它们不以 CI 成功替代，也不作为“全功能已稳定”的结论。

原生引擎另有 5 项真实模型边界测试通过；纯 Swift 跨平台 14 项通过。当前目录的后续提交仅补充报告／评测脚本，不改变上述 IPA 的 App 源码。

## 8 · 首次引导：离线／云端两种方式

- [x] 用户确认两种方式，补充 R8 需求与验收范围。
- [x] 提供 [三屏补充交互稿](design.onboarding.html)：方式选择、离线准备、云端配置，支持浅深色、返回和跳过。
- [ ] 用户确认新增引导设计。
- [ ] 替换 Onboarding 的单一 API Key 步骤，实现离线／云端分支与设置复用；保留已有配置。
- [ ] 接入 ModelScope 下载、就绪复用、蜂窝确认、失败重试及校验后启用；下载中可先进入 App。
- [ ] 统一首次引导与首页邀请标记，完成或跳过后不重复弹窗。
- [ ] 测试分支选择、跳过、保留 Key、已安装模型、下载失败及未就绪不启用。
- [ ] 真机检查键盘收起与避让、小屏／大字号、VoiceOver、浅深色；CI 结果单独回填。

本次仅需求与 HTML 原型，尚未修改 App，也未重新构建 IPA。

## 9 · 取消异常提示

- [x] 检查聊天 catch 路径，区分 Swift CancellationError、任务取消及 URLSession cancelled，保留原有内容落盘流程。
- [ ] 确认用户报错操作并复现触发原因。
- [ ] 验证主动停止、后台中断、内存压力及普通网络失败；iOS 编译／真机尚未执行。

## 10 · 后台下载

- [x] 补充 R10；查明原实现进入后台主动 pause 且采用 ephemeral session。
- [x] 实现稳定后台会话、两个文件同时提交、任务重连与进度恢复。
- [x] 接入 AppDelegate 后台事件完成回调，移除退后台暂停下载。
- [x] 保存待校验文件，前台校验后启用；暂停／删除／过期回调隔离。
- [x] 添加恢复决策、任务标识、进度与取消边界单测并执行可用检查。
- [ ] 真机验证锁屏、切后台、系统回收重启、蜂窝限制、暂停续传、删除；iOS CI 另行记录。

### R10 本地验证 · 2026-09-18

- `scripts/local-milo/test_portable.py`：21 项通过（原 14 项 + 后台下载策略 7 项），0 失败；覆盖两文件进度、旧安装任务拒绝、完整文件仍须校验、无任务不自动重新下载。
- iOS 测试增加后台 session 配置检查：固定且隔离的 Wi-Fi／蜂窝标识、蜂窝许可、后台唤醒与非 discretionary；取代已删除旧前台 transfer 的取消测试。此项尚未在 Xcode 执行。
- Swift 语法检查及 `git diff --check` 通过；另用替换 Apple 专有 API 的 Linux 桩做结构类型检查通过，不代表 iOS SDK 编译或后台会话实际运行通过。
- App 后台仅取消哈希校验与推理，系统下载继续；下载临时文件在 delegate 返回前同步移动，后台 completion 在事件交付之后执行。用户删除先撤销 ready 与安装代际，迟到回调不再接收。
- 本轮未提交／推送／打包。真机锁屏、系统回收、强退、断网、蜂窝及恢复仍待验证。

R8 补稿的 Chromium 检查通过：资源、页面定位、下载／空 Key 提示、浅深切换和 390px 无横向溢出；已查看浅色三屏截图。引导稿仍待确认。

## 11 · 宣传演示闪烁

- [x] 定位每 4 秒淡出黑场与 3 fps 预览的问题。
- [x] 修复渲染脚本，重新生成 MP4／GIF／封面。
- [x] 检查帧亮度、过渡帧、时长与循环，并更新验证记录。

### 本次 IPA 与视频验证

- 0.4.12（18），源码 `90c0e4f3a3e741017628e0519f383c6f3046b9d9`：[CI](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35321866383) 全部成功，386 项 iOS XCTest、0 失败；后台 session 配置测试已运行。真机后台调度仍待验证。
- [IPA 下载](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/35321866383/artifacts/10538105926)，为需侧载重签名的安装包。
- R11：MP4 24 秒／24 fps／576 帧；GIF 24.01 秒／12 fps／288 帧。视频最暗帧平均灰度 171.44/255，相邻帧最大平均灰度变化 5.39；无整帧黑场。已查看首个过渡前／中／后三帧，结尾交叉溶解回到第一幕。
- 视频修复发生于 IPA 构建期间，仅影响 README 演示媒体及生成脚本，不影响已构建的 App；视频修复随本次提交保存；尚未推送。
