# 第 28 轮 · 推理迁移遗留清理

用户要求直接在 main 分支清理 App 仓库与 MiloInference 职责重叠的遗留内容。

## 范围与验收

- 删除已退出正式构建的 llama.cpp 桥接、构建脚本、三个 native 验证脚本及依赖旧 C 接口的语料评估执行器。
- 删除仅服务于旧运行时的许可文件和工程排除配置；保留当前 MLX、MLXLM、Qwen 许可。
- 修正 README 的构建入口及私有包接入说明中的过时状态，明确唯一依赖来自 project.yml。
- 保留历史轮次文档和验证产物，并说明旧工具已退役；保留 Vendor 忽略规则以防旧本地产物进入版本库。
- 保留 App 调度、下载、提示词、思考解析及集成测试，不调整运行行为，不修改 MiloInference 依赖版本。
- 检查活动代码与构建入口无旧链路引用，验证工程配置、依赖解析测试和现有资源检查。

无 UI 变更。用户追加授权提交并推送 main；本轮不打包。本地检查不替代 iOS CI 和真机验证。
