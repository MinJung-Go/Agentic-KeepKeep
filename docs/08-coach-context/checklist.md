# AI 教练超长上下文实施清单

状态：实现及自动化验收完成；最终 271 个测试通过，按授权合并 main。需求与验收基线见 [requirements.md](requirements.md)。

## 已完成：准备工作

- [x] 推送当前 main 的首页图文记录修复及验证文档，基线 `fdfdef0ed7dfcff259eade1dc8530d19655efbfb`。
- [x] 创建 `feat/coach-context-management` 分支。
- [x] 核对现状：全量聊天、业务聚合、卡片历史缺口、无超限压缩重试。
- [x] 创建本轮需求文档与实施清单，更新文档索引。
- [x] 准备阶段仅完成文档；现已获授权实现并合并 main，继续不打 tag、不生成 IPA。

## A. 输入预算（FR1）

- [x] 定义可注入预算策略和 token 估算接口，集中管理候选参数及误差说明。
- [x] 计入系统指令、工具、业务概况、记忆、历史及本轮输入。
- [x] 预留实际输出与思考额度、安全余量；处理未知模型、无效预算和模型切换。
- [x] 流式/非流式入口复用规划结果，发送前二次校验。
- [x] 单条超长输入保留原文并提示，禁止静默截断。
- [x] 测试预算边界、中英文/长 JSON、工具占用及固定内容已超限。

## B. 历史压缩与记忆（FR2、FR4）

- [x] 按完整轮次保留近期对话，形成受预算约束的请求。
- [x] 定义记忆结构、来源消息范围、会话标识和摘要版本。
- [x] 实现预算内分段摘要与增量更新，复用有效缓存。
- [x] 保留用户限制、最新纠正、未决问题和决定；过滤思考过程，隔离摘要中的指令。
- [x] SwiftData 存储与旧数据兼容，失败不覆盖有效记忆。
- [x] 清空/停止/会话失效时取消任务，阻止过期结果回写。
- [x] 测试 10/100/500 轮、重启、增量水位、冲突事实、清空竞态与迁移。

## C. 计划状态（FR3）

- [x] 构建当前计划及相关训练日/动作的预算内摘要，标注省略范围。
- [x] 保留无正文工具卡片的语义，区分草稿、已确认、拒绝和已替换状态。
- [x] 数据库最新计划优先于历史记忆，正确解析相对日期。
- [x] 验证确认后追问、计划编辑/替换和大课程表；不重复执行工具。

## D. 超限与故障恢复（FR5）

- [x] 映射两种协议的明确超限错误，普通错误不触发压缩重试。
- [x] 首次超限且未输出时最多重试一次，复用同一用户消息身份。
- [x] 摘要失败使用有效缓存/预算内历史；关键内容容纳不下时提示并保留输入。
- [x] 部分输出、工具 JSON 截断及用户停止不触发重复生成/执行。
- [x] 测试超限成功恢复、再次超限、鉴权/额度/网络错误、取消及摘要失败。

## E. 界面、用量与质量（FR6）

- [x] 整理上下文状态与现有流式/停止操作衔接。
- [x] 摘要调用计入现有用量统计，短聊天无额外摘要调用。
- [x] 日志仅记录诊断指标，验证聊天/健康信息和 Key 不进入日志。
- [x] 对长历史估算、缓存命中、延迟和内存做测量，记录设备与样本。
- [x] 使用受控长对话检查目标、限制、计划状态和用户纠正的保留质量。

## 验证记录

| 项目 | 当前结果 |
|------|----------|
| 首轮功能提交 | `0352006`：构建成功，266 个测试通过 |
| 首轮 CI | https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/34805114393 |
| 最终代码提交 | `9da7164397268df8de6c53dcce62590adb7ba81e`：构建成功，271 个测试、0 失败；[最终 CI](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/34805842367) |
| 旧数据 | 使用旧 ChatMessage 模型创建磁盘数据库，再用新模型打开，迁移测试通过 |
| 性能与估算参数 | 见 [verification.md](verification.md)；合成数据与模拟客户端，不代表真实接口耗时 |
| 真机与真实模型 | 尚未验证，见下方人工复验项 |
| IPA | 本次不构建 |

## 人工复验（未冒充自动化通过）

- [ ] 用实际 OpenAI 兼容端点与 Anthropic 模型检查长对话回答是否遵循新目标、伤病限制和计划状态；自动测试验证的是请求内容及来源约束。
- [ ] 真机体验整理提示、停止、清空、退出重进，以及输入失败后的原文保留。
- [ ] 测量真实模型首次整理 100/500 轮的网络延迟、费用与真机内存；目前只有模拟器本地规划测量。

上述人工项是后续设备/服务商验收记录，不影响本轮已覆盖的纯逻辑、持久化与迁移测试结论。
