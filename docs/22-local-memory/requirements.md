# 第 22 轮 · 正式 App 纯文字 L14

## 现象与证据
用户报告正式 App 发送纯文字“你好”后收到 L14。独立 Lab 短文本已由用户验证通过。当前没有失败阶段、实际输入 token 或内存采样，不能确定是加载峰值、提示词 prefill 还是其他 App 内存占用。

代码确认 CoachContextEngine 默认给所有请求添加课程表工具和查询工具；短问候也可能包含大量附加文本。8K 是上限，不等于每次预分配满 8K，不能单凭该配置认定根因。

## 本轮处理
1. 用户否定纯问候精简方案，已撤回；保留原有角色、历史、工具和请求预算。按用户要求切换到更小的 Qwen3.5-0.8B MLX 4bit 图文模型，不通过针对问候删减上下文规避故障。
2. L14 错误增加阶段、LLM／VLM 路径、实际输入 token（尚未计算则明确显示）、本次采样进程峰值；不记录提示词、回复、个人记录或 API Key。不新增界面，复用现有错误区域。
3. 保留内存警告停机与首因；不得通过忽略警告、静默云端回退或自动重试掩盖故障。未增加 64K。
4. GPU 已使用时，释放前等待默认 GPU stream 完成，避免已发出的计算与下一请求加载重叠。

## 验收
保持原请求构建路径；诊断字段／未知值／隐私边界测试，既有回归。真实 L14 是否消除需同设备复测，若仍失败依据阶段继续定位；不预先宣称已修复根因。

## 选定模型 · 2026-09-20
ModelScope `mlx-community/Qwen3.5-0.8B-4bit`，元数据 revision `f607c492a6aeeb50ca73cca0b924c006331a02af`，`model.safetensors` 625,229,487 字节（现用 2B 权重 1,722,271,785 字节）。为图文 MLX 4bit 转换；文件体积不等于实际运行内存。本轮替换完整下载清单、型号显示与许可；按 revision 隔离安装目录和后台任务身份。旧 2B 不能被识别为新版已就绪，也不自动删除旧文件或用户记录。独立 Lab 保留 2B 基线。加载、工具、图文质量仍需真机验证。
