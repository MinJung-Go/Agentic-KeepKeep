# 训练执行页 —— 实施 Checklist

- 日期：2026-09-14
- 依据：[需求文档](requirements.md) · 视觉参考：[04 轮 SCREEN 02](../04-ui-polish/exercise-illustrations.html)
- 状态：**已实施**（待真机复验）
- 约定：每批 push 后等 CI 绿再打勾，打勾时附提交号与单测数

## 批次 A — 数据模型与会话引擎（FR6 / FR3）

> 先做这批，因为它独立于 UI、可直接用单测验证。也是本项目**第一次改数据模型**。

- [x] `SetLog` 模型：`setIndex` / `weightKg` / `reps` / `completedAt` / 关联
- [x] `WorkoutSessionDraft` 模型：`planDayUUID` / `startedAt` / 已完成的组；结束时转正并删除
- [x] `WorkoutSessionEngine`（纯逻辑）：当前动作 / 当前组 / 完成一组 / 跳到某动作 / 汇总
- [x] 从 `PlanExercise.setsText`（「4×10」）解析出组数与次数，解析失败不编造
- [x] 单测：推进顺序、跳动作、多做/少做、汇总数字、空计划、解析失败

## 批次 B — 训练执行页（FR2 / FR4）

- [x] `WorkoutSessionView`：顶部「第 N / M 个动作」+「已练 X 分钟」
- [x] 当前动作卡：示范图 **140pt**（复用 `ExerciseMediaView`）+ 名称 + 组次 + 肌群胶囊
- [x] 动作名可点进详情页，返回不丢进度
- [x] 逐组列表：每组一行，勾选圆点
- [x] 点某组展开改重量/次数（默认沿用计划值，不强制输入）
- [x] 底部主按钮「完成这组」；最后一组做完自动切下一个动作
- [x] 上一个 / 下一个动作可跳

## 批次 C — 入口、写回与边界（FR1 / FR5 / FR7）

- [x] 入口：今日页课程卡主按钮改「开始训练」
- [x] 入口：课程表 · 单日页底部常驻「开始训练」
- [x] 结束汇总卡：时长 / 动作数 / 组数 / 总容量
- [x] 写回：生成 `WorkoutSession` + `ExerciseSet`，训练日标为已完成，删除草稿
- [x] 中断恢复：重进时提示「继续上次的训练」，可继续或结束
- [x] 过期草稿清理（超过 24 小时视为放弃）
- [x] 边界：素材未下载 / 动作未收录 / 计划日无动作 / 计划没写重量

## 验证记录

| 日期 | 提交 / 标签 | 结果 |
|------|------|------|
| 2026-09-14 | 批次 A `4f19ab3` · B/C `7d37045` · 落库测试 `bb5991b` | 批次 A/B/C 完成，CI ✅（run 34771795369）；**239 个单测全绿**（新增 31 例）；真机项待验 |

## 真机复验要点

> 以下**未验证** —— 都需要装机才能确认。

- [ ] 跟着计划走完一次训练，全程不输入也能完成
- [x] 锁屏 / 切到别的 App 再回来，勾选状态不丢
- [x] 杀进程重进，提示「继续上次的训练」
- [x] 练完生成的记录在记录页里显示正常，与手敲的记录样式一致
- [x] 旧数据（04 轮及以前产生的记录）升级后能正常打开
- [x] 示范图未下载时占位说得清楚，不阻断训练
- [x] 「已练 X 分钟」在后台一段时间后回来仍然准确
