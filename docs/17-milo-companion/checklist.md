# Milo 运动伙伴清单

状态：已按用户指示开发，代码、Xcode 编译与模拟器单测已通过；真机验证待进行。已提交、推送并成功构建 IPA 0.4.4 (8)，结果见文末。

## R1 · 角色与相处方式

- [x] 确认默认大姐姐、可选大哥哥及表达边界。
- [x] 实现设置二级页面、角色预览、昵称与表达偏好持久化。
- [x] 每次请求读取最新角色设置，不改写历史对话、目标或记录。
- [x] 角色只影响表达，课程修改与删除继续经过既有确认流程。
- [x] 聊天页使用自定义名称与已认可的 Milo IP。
- [x] 添加默认值、重新读取、长度边界与提示词权限边界测试。
- [ ] 真机确认设置页面、键盘收起、角色切换与昵称显示。

## R2 · 思考与工具时间线

- [x] 按真实时序穿插各轮思考与工具事件，统一置于最终回复上方。
- [x] 每轮仅显示服务商返回的可展示思考，无返回则不补造；遵循显示开关。
- [x] 进行中可展开，结束后整体折叠；已有步骤可回看，中断保留内容与状态。
- [x] 保存有序事件，兼容旧版工具记录与独立思考字段；不加入后续对话上下文。
- [x] 纯改期保存撤销凭据，支持恢复日期与备注；拒绝覆盖后续编辑、拒绝重复撤销；删除不提供改期撤销。
- [x] Xcode 运行 3 项新增撤销测试（恢复、版本冲突、删除边界），全部通过。
- [x] 输入栏保留唯一停止入口，删除回复下方重复停止按钮。
- [x] 节流思考更新，使用真实耗时，避免历史思考时间继续增长。
- [x] 添加多轮顺序、持久化编码、旧记录兼容、无思考与中断状态测试。
- [ ] 真机验证长思考展开、流式滚动、键盘以及取消状态。

## R3 · 动作教学视频推荐

- [x] 确认四屏设计：计划补充进度、教学卡片、备选与异常状态。
- [x] 实现受约束的公共动作检索、器械与变式筛选、链接验证和结果排序。
- [x] 先生成计划，再后台补充；最新待确认计划和已保存有效计划均支持。
- [x] 每批最多 12 个动作，每动作 1 次搜索、最多 2 次网页读取、最多保存 3 个来源；可继续补充。
- [x] 同动作复用 SwiftData 缓存；30 天有效期、失败退避、手动重试；媒体及网页正文不入库。
- [x] 动作详情提供教学卡片、原站跳转、备选更换与本地失效反馈；草稿动作也可进入详情。
- [x] 展示实际搜索／网页阅读事件；读取失败不冒充已阅视频，作者未知时不编造。
- [x] 搜索中、暂无结果、失败、停止、联网关闭明确区分；失败保留已有链接和课程。
- [x] 停止批次终止后续请求，取消或配置变化后丢弃迟到结果。
- [x] 缓存使用独立 ModelContext 保存，失败回滚，不连带保存未完成的课程编辑。
- [x] 添加匹配／链接筛选、去重预算、选择和失效标记、关闭联网、取消、失败及阅读顺序测试。
- [x] Xcode 运行 8 项教学 SwiftData / 流程测试，全部通过。
- [ ] 真机验证从旧版升级的数据库迁移、重启后的持久化和界面更新。
- [ ] 真机验证原站跳转、课程自动补充、深浅色与无网络体验。
- [ ] 使用实际 GLM 账号验证搜索与阅读接口、来源可用性和推荐质量；本轮未发出付费 API 请求。

## 验证记录

- 本轮新增 18 项 XCTest：角色／时间线／来源策略 7 项、教学存储与流程 8 项、改期撤销 3 项。
- Linux SwiftPM 可移植验证：56 项通过，0 失败（包含上述前 15 项；3 项撤销测试另已在下述 Xcode CI 通过）。网络均为测试夹具；存储流程用内存适配器替代 SwiftData，所以不代表真实数据库／迁移验证通过。
- 全仓 App 与测试共 124 个 Swift 文件语法解析通过；语法检查不等于 iOS 类型检查或编译。
- 第三方动作数据集检查、git diff --check 通过；只加入已认可的原创 Milo PNG，无教学视频、封面或网页正文资源。
- 本地 Linux 无 Xcode / iOS SDK；已通过 GitHub macOS runner 完成完整 iOS 编译与模拟器单测，未进行真机测试。
- 成功 CI 提交：`0775361`，[Actions 34954218472](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/34954218472)，334 项真实 iOS 模拟器测试通过、0 失败；覆盖本轮全部 18 项新增用例。

## IPA 0.4.4 (8) 构建跟踪

- 首轮 CI：265bd76 / run 34952820089。Xcode 编译、侧载权限检查通过；334 项模拟器单测中 333 项通过。
- 唯一失败为旧 CoachStreamingTests 仍要求关闭思考时省略参数，与第 14 轮已确认的显式 false 行为不一致。已修正断言为 false，保留传输层 disabled 编码测试，完整重跑已通过。
- 本轮新增角色、教学 SwiftData 与改期撤销测试均已在真实 iOS 模拟器通过；最终 CI 与 IPA 链接见下文。

- 第二轮：`0775361`，334 项单测全部通过，IPA 已归档完成。后续 `356f0d6` 仅补推第 12 轮文档改名，不改变本包应用代码。

### 最终产物

- [成功流水线（第 2 次尝试）](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/34954218472)：334 项单测通过、0 失败，设备 Archive、资源检查、侧载权限嵌入、IPA 打包全部成功。
- 首次 IPA job 未启动：GitHub 连续 5 次无法分配 runner；仅重跑失败 job 后成功，不涉及应用代码修改。
- [下载 IPA Artifact](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/34954218472/artifacts/10390944447)，解压后使用 `KeepKeep-unsigned.ipa`，需侧载工具重签名。
- 已下载核验：版本 `0.4.4`，构建号 `8`，Bundle ID `com.minjung.keepkeep`，ZIP 完整性通过，无旧动作数据集/GIF 资源。
- IPA SHA-256：`67704371d2563506a4b9743c1884b46748a0e675a648fa732e8e76e5ae0ca0a6`。
- 应用与测试构建提交 `0775361`；后续补推均为文档变更，不影响包内代码。真机体验、旧版升级与实际 GLM 来源质量仍待验证。
