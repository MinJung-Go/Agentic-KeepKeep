# 第 11 轮 Checklist

原开发分支：`feat/coach-query-tools`，已整合至重建仓库的 `main`。后续动作详情修复分支：`fix/plan-exercise-details`。

状态更新：2026-09-15。主分支 IPA 已生成；后续动作详情与误删修复已推送至功能分支，Xcode 构建及 300 项单测通过，已合入 main 并生成 0.4.3（7）IPA。

## 实现

- [x] 创建需求文档与独立分支
- [x] 本地只读聚合查询：健康、训练、饮食、身体指标，单次最多31天、每类最多2000条
- [x] 课程表索引与指定计划/日期查询，提供固定ID和版本；支持未来排期
- [x] 教练工具循环：最多2批、每批最多2次查询；每轮联网最多1次，后续保留计划草稿工具
- [x] 查询错误、取消、输出大小及总上下文预算保护；付费搜索不因后续溢出被重放
- [x] 移除默认28/14天概况；临时工具消息不写入持久聊天或记忆
- [x] 所有模型请求（含重试）注入隐藏的当前时间、日期、星期、时区
- [x] 保留GLM reasoning_content、Claude签名/加密思考块以完成工具后续请求；协议块不渲染、不落聊天历史
- [x] GLM搜索默认关闭、官方端点校验、固定公共主题词、最多3个来源、错误降级
- [x] 搜索来源链接、网络响应与摘要长度限制、禁止携带Key跳转
- [x] 设置首页外观：跟随系统/浅色/深色，即时持久化；移除Info.plist强制深色
- [x] AI课程表真实修改：主题、动作/组次/重量、日期、减载、跳过、增删训练日、删除整份课程表
- [x] 课程表变更按固定ID和版本校验，先完整校验再保存，错误回滚，保留回执并防重复执行
- [x] 手动删除当前计划及管理全部计划入口；删除保留训练记录、健康数据、进行中训练草稿
- [x] 删除当前计划后不自动展示归档计划为当前计划；同一天多个训练日在课程表中完整展示
- [x] 新增查询、时间、搜索、外观、课程表变更回归用例

## 已执行验证（2026-09-14）

- [x] `git diff --check`
- [x] Swift 6.0.3 Linux编译器语法检查：112个Swift文件通过
- [x] Info.plist解析通过，确认无强制外观属性
- [x] 临时Linux SwiftPM离线测试：33项通过、0失败（含10项既有协议测试）
- [x] 数据查询/课程表变更逻辑在平台类型占位下通过Swift类型检查，仅作辅助，不代表SwiftData运行验证

离线测试目录 `/tmp/keepkeep-swift-check/harness`，日志 `/tmp/keepkeep-swift-check/test.log`。
测试使用生产逻辑副本，Apple日志被移除、记忆/CryptoKit依赖使用占位，URLSession.AsyncBytes用本地URLProtocol回放适配。覆盖工具循环、预算、取消/失败、JSON编解码、时间/时区、日期边界、搜索来源及签名块拼接。**不覆盖SwiftData级联保存、SwiftUI渲染、真实流式网络时序或完整记忆压缩。**未使用真实API Key，未发生付费搜索。

## CI 与真机验证

- [x] Xcode构建与完整单测：cf9a3f4，Actions 34852151169，296项通过、0失败
- [ ] SwiftData：健康/历史记录查询，修改计划保存、批量校验、版本冲突、删除保留训练日志
- [ ] 真机：今日/昨日步数、历史月份、缺失数据与联网开关/来源链接
- [ ] 真机：GLM实际工具循环及联网API；Claude签名续传
- [ ] 真机：系统/手动外观切换、聊天与记录弹窗、重启保留
- [ ] 真机：修改动作/日期、增加训练日、删除训练日/计划、旧建议失效、回执与重复确认
- [ ] 真机：删除当前计划后首页空态、管理页删除归档计划，进行中训练保留

用户已要求整合至 main 并重建远端仓库，执行结果见后续记录。

## 追加：移除 Gym 数据集（2026-09-14）

- [x] 删除本地 161 个 GIF、目录 JSON 及数据集专用加载/播放代码
- [x] 删除构建下载脚本及历史设计稿素材下载脚本
- [x] 动作详情、训练中卡片移除示范/肌群/数据集说明，保留计划名称、组次、重量与进度
- [x] 设置页移除素材数量与署名，README 更新构建步骤
- [x] XcodeGen 排除旧资源路径；CI 在源码检查及归档后、IPA 复制前检查无旧目录/JSON/GIF
- [x] 保留教练使用的名称归一化工具，3 个 Swift 可执行用例通过
- [x] 资源守卫 5 个场景通过（空目录、旧目录、目录 JSON、GIF、路径不存在）
- [x] 源码资源检查通过；110 个 Swift 文件语法检查通过（含 Widget）；git diff --check 通过
- [x] 核对修改后的资源排除项及 CI 检查执行顺序；手绘样张目录保持原样
- [x] XcodeGen/Xcode 构建、完整单测：cf9a3f4，296项通过
- [x] IPA 归档产物检查：2026-09-14，`b01e420`，[Actions 34856662855](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/34856662855) 全流程通过；下载后 ZIP 完整性检查通过，确认无旧动作目录、目录 JSON 或 GIF
- [ ] 真机核对动作详情、训练中页面与设置页

既有测试数量将因移除图库功能专用测试而减少。未修改已有课程表或训练记录。后续仓库重建状态见下。

## 仓库重建

- [x] 用户手动删除旧 GitHub 仓库后，将全部当前改动在本地快进合入 main（旧远端已不存在，无法创建 MR）
- [x] 从 main 的源码树新建仓库，不导入旧 Git 历史、分支、标签或 IPA
- [x] 同名公开仓库 MinJung-Go/Agentic-KeepKeep 重建，初始提交 cf9a3f4
- [x] 本地切换为新仓库历史，仅保留 main；原本地 Git 元数据备份在工作区外，不上传
- [x] 全树资源检查无旧动作目录、JSON 或 GIF；设计稿内嵌 PNG 与现有 App Logo 哈希一致
- [x] 新仓库首次 CI 的资源检查、XcodeGen 工程生成通过
- [x] 新仓库完整构建与单测：[Actions 34852151169](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/34852151169)，296项通过、0失败；侧载权限检查通过，IPA任务跳过

仓库重建时未触发 IPA；随后按用户要求从 `main` 手动打包成功，版本 0.4.2（6），约 2.76 MiB。新仓库未恢复旧 Release/Actions 产物。仓库重建不代表对全部素材的版权授权保证，也无法删除第三方已保存的副本。

## 追加：动作详情与返回后动作消失（2026-09-15）

详细记录：[第 13 轮修复清单](../12-plan-detail-fix/checklist.md)。

现象：详情只有动作名称，返回训练日后动作消失。已确认详情在移除图库后缺少参数缺省提示；行内导航与默认样式删除按钮存在误触风险，但“返回后消失”的实际触发原因仍待真机复现确认。

- [x] 动作详情显示组次、重量及可用的 RPE；缺失参数明确显示“未设置”
- [x] 课程表动作支持独立编辑草稿，保存后写入，取消不修改原数据
- [x] 名称、重量校验，保存失败反馈；编辑页提供收起键盘入口
- [x] 动作行只保留导航，按固定 UUID 标识；删除移到左滑菜单，禁用全滑触发并二次确认
- [x] 删除同步更新训练日关系，失败回滚；确认前不使用 destructive 角色触发行移除动画
- [x] 清理“查看示范”的旧入口文案，不恢复第三方图库
- [x] 新增 4 项 SwiftData 回归用例：丢弃草稿、保存参数与关系、非法输入不部分写入、删除仅影响指定动作并保留训练历史
- [x] 本地 112 个 Swift 文件语法解析、`git diff --check`、旧数据集资源检查通过
- [x] 新修复的 Xcode 构建及完整单测：`6d02601`，[Actions 34926902717](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/34926902717)，300 项通过、0 失败；新增 4 项 PlanExerciseStoreTests 均实际执行通过
- [x] 新修复已提交并推送至 `fix/plan-exercise-details`
- [x] 新修复合并至 main 并生成 0.4.3（7）IPA：`6e14e69`，[Actions 34933383381](https://github.com/MinJung-Go/Agentic-KeepKeep/actions/runs/34933383381) 全流程通过；此前 `b01e420` 安装包不含本次修复
- [ ] 真机：逐个打开动作再返回，确认动作不消失；取消删除保留动作，确认删除仅移除目标动作
- [ ] 真机：编辑取消保持原值，保存后返回及重启保留参数，空值/零重量显示正确

本地环境无 iOS SDK；本次已通过 GitHub macOS runner 执行 Xcode 构建和 iOS 模拟器 XCTest。首次 CI 检出编辑草稿与 AI 草稿类型重名，已将新类型改为 `PlanExerciseEditDraft` 并重新验证通过。单测不替代真机点击、返回和键盘交互验证。
