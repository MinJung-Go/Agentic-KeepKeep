# 本地 16K checklist

- [x] 需求确认：默认 16K；32K 在真机验证后再开放。
- [x] UI 范围：仅更新既有模型容量文案，无新增交互。
- [x] 统一请求预算、tokenizer 校验与 MLX KV 窗口为 16K。
- [x] 同步模型信息，保留云端、摘要、查询工具与内存保护。
- [x] 更新边界及模式切换回归，运行可用测试与语法检查。
- [ ] iOS CI 编译与全量单测（本轮尚未提交）。
- [ ] 真机先复测已发布 0.8B / 8K 的“你好”，再验证 16K 长历史、工具与单图。
- [ ] 32K 真机验证、选择入口设计确认与开发（后续阶段）。

## 验证记录
- Swift 6.0.3 portable XCTest：31 项通过，0 失败（其中新增 2 项预算边界测试）。
- Swift 语法解析与 `git diff --check` 通过。
- 新增 iOS 模式切换预算回归，既有工具请求用例更新为 16K 并检查查询工具仍完整；这部分 XCTest 待 macOS CI 执行。
- 当前改动未提交、未推送、未打包；未借用上一版 397 项 CI 结果作为本轮证明。
- 未更换模型文件，无需因上下文扩容重新下载模型。
